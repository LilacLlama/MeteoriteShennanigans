# Copy this to .env and fill in your values before running scripts/bootstrap.sh
# .env is gitignored — never commit it.

APP_NAME=meteorite-explorer
AWS_REGION=us-east-1

# Optional — override the Bedrock model used by the agent when re-enabled
# BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-5-20250514-v1:0
